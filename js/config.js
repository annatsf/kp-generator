// config.js — одноразові налаштування. Дивись README.md, розділ
// "Google Cloud / Sheets API" — як отримати GOOGLE_API_KEY.
window.KP_CONFIG = {
  // Ключ Google Cloud API з увімкненим Google Sheets API (лише читання).
  // Той самий проєкт можна перевикористати з ses-calculator (там уже є
  // ключ для Maps API) — просто увімкни там ще й "Google Sheets API".
  GOOGLE_API_KEY: "AIzaSyDrh0z0gHcxjcGVVsIeLqxe7XiNr9gL_ls",

  // Назви вкладок у файлі-розрахунку — мають співпадати з реальними
  // назвами вкладок у Google Sheets. Якщо назви вкладок трохи відрізняються
  // (наприклад "ПДВ " з пробілом), онови тут.
  SHEET_TAB_PDV: "ПДВ",
  // Друга вкладка номенклатури — для розрахунків без ПДВ (запит Анни,
  // 2026-07-18: іноді клієнт просить прорахувати варіант без ПДВ). У формі
  // на сайті це просто перемикач "ПДВ" / "C" — слова "готівка"/"ФОП" ніде
  // в інтерфейсі чи згенерованому документі НЕ показуються, лише тут,
  // в конфізі (бо так реально називається вкладка у файлі-розрахунку).
  SHEET_TAB_CASH: "Готівка_ФОП",
  SHEET_TAB_MODEL: "Моделювання Фін. показників роботи СЕС",
    // Назва об'єкта береться з комірки A1 цієї вкладки (не з назви файлу
    // Google Sheets — назва файлу часто службова/технічна).
    SHEET_TAB_OBJECT_NAME: "Кошторис_Наявність обладнання",

  // Сторінка PvSyst.pdf (звіт з Google Drive), яку рендеримо на "04"
  // сторінку КП, і прямокутник обрізки — які частини цієї сторінки PDF
  // лишити (все в частках 0..1 від розмірів сторінки: top/left — відступ
  // від верхнього/лівого краю, width/height — розмір вирізаної області).
  // Мета — лишити лише блок "Near shadings parameter" (3D-перспектива
  // об'єкта), без блоку "Iso-shadings diagram" (полярна діаграма), що йде
  // одразу під ним на тій самій сторінці, і без шапки/підпису PVsyst.
  // ВИПРАВЛЕНО (2026-07-22): height зменшено з 0.58 до 0.36 — Анна
  // прислала реальний скріншот сторінки "04", на якому крім потрібної
  // 3D-перспективи в кадр потрапляв ще й весь блок "Iso-shadings diagram"
  // нижче. За пропорціями на її скріншоті блок "Near shadings parameter"
  // займає приблизно 63% висоти попереднього кадру (0.58 × 0.63 ≈ 0.36).
  // Це підбір за одним реальним прикладом, не точний замір з оригінального
  // PDF (немає доступу до самого файлу) — якщо після цього все ще
  // захоплює зайве знизу чи обрізає саму 3D-діаграму, зменшуй/збільшуй
  // height ще (крок ~0.03-0.05), top можна лишити як є.
  PVSYST_PAGE: 5,
  PVSYST_CROP: { top: 0.12, left: 0.03, width: 0.94, height: 0.36 },

  // BUDGET_EQUIP_ROW_RANGE прибрано (2026-07-22): раніше тут був жорсткий
  // діапазон рядків B3:B10 для позицій обладнання на сторінці "Бюджет
  // реалізації" — ламався, коли реальних позицій обладнання в файлі було
  // більше 8 (відрізало справжню позицію замість службової нотатки
  // менеджера). Замінено на фільтр за змістом назви — isStrayEquipNote()
  // в js/sheets.js і js/kp-render.js.

  // Компанія — статичні дані для футера/умов (сторінка "Що входить та умови").
  COMPANY: {
    name: 'ТОВ «ЕСКОРЕ»',
    tagline: "Сонячні електростанції під ключ\nдля дому та бізнесу — Одеса та область",
    address: "65000, м. Одеса, вул. Леонтовича, 16-а",
    phone: "+38 075 410 00 16",
    email: "info@escore.com.ua",
    site: "escore.com.ua",
  },

  // Менеджер клієнтського супроводу — показується на останній сторінці КП
  // (персональна контактна картка з фото, запит Анни, 2026-07-13). Поки
  // один менеджер ("пока один менеджер" — її слова) — якщо з'являться
  // інші, це має стати масивом MANAGERS + випадаючий список вибору на
  // стартовій сторінці (form) замість жорсткого посилання на єдиний
  // об'єкт тут. Фото — assets/manager-albina.jpg (завантажено і
  // скомічено в repo, оригінал прислала Анна).
  MANAGER: {
    name: "Кириченко Альбіна",
    position: "Менеджер клієнтського супроводу",
    photo: "assets/manager-albina.jpg",
    email: "info@escore.com.ua",
    phone: "+38 075 410 00 16",
    address: "Одеса,\nвул. Леонтовича 16 А",
    instagram: "@escore.ua",
    facebook: "escore",
  },

  // Бізнес-правила за замовчуванням (можна перекрити у формі перед генерацією).
  DEFAULTS: {
    vatRate: 0.20,
    prepaymentPct: 70,
    validDays: 14,
    warrantyMonths: 24,
    leadTimeWeeks: "6–8",
    tariffUsdPerKwh: 0.33,
  },
};
